# Secrets — never commit
cookies.txt
.env

# Output
songs.csv
data/

# Calibration artifacts (disposable, regenerated by --calibrate)
calibration_frame.png
calibration_crop.png
pink_mask.png

# Per-cycle debug screencaps (auto-pruned after DEBUG_KEEP_HOURS)
debug_caps/

# Env / tooling
.venv/
.idea/
__pycache__/
