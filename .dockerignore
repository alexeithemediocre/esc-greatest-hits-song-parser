# Only pyproject.toml, uv.lock and main.py are COPY'd, but keep the build
# context small and make sure secrets/output can never leak into the image.
.git
.idea
.venv
data/
cookies.txt
songs.csv
*.png
CLAUDE.md
Dockerfile
docker-compose.yml
